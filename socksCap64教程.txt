为什么？

Shadowsocks客户端本身只能代理到网页（通常指浏览器）。所以电脑上其它软件无法使用这是很麻烦的事情，或者用于其他某些用途。
这个教程就是教大家如何使用SocksCap64进行全局代理。

使用方式：

该软件主要有两种使用方式。

使用SocksCap64内建的Shadowsocks（不支持SSR）连接方式进行配置。

SocksCap64配合专用的Shadowsocks（ShadowsocksR）客户端进行配置。

一、SocksCap64内建Shadowsocks方式：

打开SocksCap64客户端，首次打开会提醒导入浏览器，点击 确定/取消 皆可

1.如图所示点击 代理 按钮

添加代理

2.点击左下方的加号来添加代理配置，或点击SS图标扫描二维码等方式添加。

添加代理配置

代理类型选择Shadowsocks，填入服务器地址、端口和修改加密方式为chacha20。

socks1

右键 刚才添加的代理，如图所示分别测试两种协议是否可用。
socks3

如果两种协议测试完成都显示如图所示，即代表可以正常代理。然后点击右下方的 保存 按钮来保存配置。

socks4

4.此时将你需要代理的软件图标拖拽进SocksCap64主界面，并选中成蓝色，点击运行即可。

socks5

二、Shadowsocks客户端+SocksCap64方式：

使用该方式配置时Shadowsocks**（ShadowsocksR）客户端必须开启**。

1.打开SocksCap64客户端，首次打开会提醒导入浏览器，点击确定即可，如图所示点击代理按钮。

这里写图片描述

2.点击左下方的加号来添加代理配置。

这里写图片描述

3.修改IP为 127.0.0.1，1080端口，代理类型 sock5，空账号，空密码

这里写图片描述

4.右键刚才添加的代理，如图所示分别测试两种协议是否可用。

这里写图片描述

如果两种协议测试完成都显示如图所示，即代表可以正常代理。

这里写图片描述

5.随后点击保存来完成代理配置的添加。

这里写图片描述

6.然后将你需要代理的软件图标拖拽进SocksCap64主界面，并选中成蓝色，点击运行即可。

这里写图片描述

软件弊端：

此软件有个小弊端，因为部分软件的运行程序与软件的快捷方式（软件主体）并不处在同一个程序内，也许只是一个启动器的作用，此时就非常的麻烦，因为你并不知道还要添加什么进来才可以将此软件全部代理。尤其是游戏，下面教大家一种方法，来强制将所有软件都进行代理。

解决方法：

打开SocksCap64，然后点击上方的配置。

q-1

点击网络规则。

这里写图片描述

点击下方的【添加】来添加配置。

这里写图片描述

填写一个名称，目标主机留空，目标端口填写 1-65535 ，运作选择代理，然后一路确定，返回到SocksCap64主界面运行代理（操作与教程相同，必须相应程序来开启代理），此时你应该可以尝试进入游戏（软件）测试是否成功代理了。

这里写图片描述